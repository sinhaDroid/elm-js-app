- update the model to keep track if user is logged in or not.
- add login and logout events.
- update view such that if user is logged in, they see the counter UI, else
  they see a login screen. add logout button to counter.
- implement the update function to make it work
